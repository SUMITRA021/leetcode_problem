class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector<int>ans;
        int n=nums.size();
        int i=0;
        while(i<n)
        {
            int val=nums[i];
            ans.push_back(nums[val]);
            i++;
        }
        return ans;
    }
};
